function filt = makeArrayFilter(inputFilter,n)
% Makes array filter for filtering 2D matrix of 1D signals

% n = number of rows to add

tmp = inputFilter(:);
filt = inputFilter(:,ones(n,1));
    

end